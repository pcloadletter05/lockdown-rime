---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/nt4.css
  - public/index.html
  - public/desktop.html
  - public/js/time.js
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05
  - VIS-06
user_setup: []

must_haves:
  truths:
    - "All UI chrome uses #C0C0C0 silver with 4-tone beveled borders via box-shadow inset"
    - "Active title bars are solid navy #000080, inactive are #808080, no gradients anywhere"
    - "Window buttons are flat beveled rectangles with CSS-drawn glyphs"
    - "UI font is W95FA at 11px with Tahoma fallback and font-smoothing disabled"
    - "Desktop background is teal #008080"
    - "Viewport under 800x600 shows NT4-styled error dialog gate"
    - "TimeEngine maps real clock to December 23 1999 with 1:1 hour mapping"
  artifacts:
    - path: "public/css/nt4.css"
      provides: "Complete NT4 visual system with CSS custom properties and bevel classes"
      contains: "box-shadow: inset"
    - path: "public/js/time.js"
      provides: "TimeEngine with getMappedTime, formatClockTime, formatFullDate"
      exports: "getMappedTime"
    - path: "public/index.html"
      provides: "Boot screen container with viewport gate and font loading"
      contains: "id=\"boot-screen\""
    - path: "public/desktop.html"
      provides: "Desktop shell with teal background"
      contains: "id=\"desktop\""
  key_links:
    - from: "public/css/nt4.css"
      to: "public/index.html"
      via: "stylesheet link"
      pattern: "href=\"css/nt4.css\""
    - from: "public/css/nt4.css"
      to: "public/desktop.html"
      via: "stylesheet link"
      pattern: "href=\"css/nt4.css\""
---

<objective>
Build the complete NT4 visual system in CSS, set up the viewport gate, prepare both HTML pages with correct structure, create the TimeEngine, and download the W95FA font.

Purpose: Every subsequent plan and phase depends on nt4.css being correct. The CSS is the visual foundation -- if the bevels, colors, or fonts are wrong here, everything built on top inherits the error. The viewport gate protects the experience from small screens. The TimeEngine provides the 1999 time mapping used by the taskbar clock and email time-gating.

Output: Fully styled NT4 CSS system, viewport gate in index.html, desktop.html with teal background, TimeEngine in time.js, W95FA font files in assets/fonts/.
</objective>

<execution_context>
@C:/Users/Taylor/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Taylor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download W95FA font and create nt4.css with complete NT4 visual system</name>
  <files>public/css/nt4.css, public/assets/fonts/w95fa.woff2, public/assets/fonts/w95fa.woff</files>
  <read_first>
    - public/css/nt4.css (current placeholder)
    - .planning/research/STACK.md (color palette, bevel recipes, font stacks)
    - .planning/phases/01-foundation/01-RESEARCH.md (CSS bevel recipes, color palette section)
  </read_first>
  <action>
**Step 1: Download W95FA font files.**
Download from https://fontsarena.com/w95fa-by-alina-sava/ -- get the WOFF2 and WOFF files. Place them in `public/assets/fonts/w95fa.woff2` and `public/assets/fonts/w95fa.woff`. If direct download fails, the font is SIL-licensed and available from multiple mirrors. Try the GitHub source or Google Fonts alternative.

**Step 2: Build nt4.css from scratch** with these exact sections:

**@font-face declaration:**
```css
@font-face {
  font-family: "W95FA";
  src: url("../assets/fonts/w95fa.woff2") format("woff2"),
       url("../assets/fonts/w95fa.woff") format("woff");
  font-weight: normal;
  font-style: normal;
}
```

**CSS Custom Properties (`:root`):**
All 20+ color variables from STACK.md verbatim:
- `--button-face: #C0C0C0` (NOT #D4D0C8 which is Win98)
- `--button-highlight: #FFFFFF`
- `--button-light: #DFDFDF`
- `--button-shadow: #808080`
- `--button-dk-shadow: #000000`
- `--window-bg: #FFFFFF`
- `--window-text: #000000`
- `--active-title: #000080`
- `--active-title-text: #FFFFFF`
- `--inactive-title: #808080`
- `--inactive-title-text: #C0C0C0`
- `--desktop: #008080`
- `--menu-bg: #C0C0C0`
- `--menu-text: #000000`
- `--highlight: #000080`
- `--highlight-text: #FFFFFF`
- `--scrollbar: #C0C0C0`
- `--tooltip-bg: #FFFFE1`
- `--tooltip-text: #000000`
- `--disabled-text: #808080`
- `--app-workspace: #808080`

**Global reset and base styles:**
```css
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html, body { height: 100%; overflow: hidden; }
body {
  font-family: "W95FA", "MS Sans Serif", "Microsoft Sans Serif", Tahoma, sans-serif;
  font-size: 11px;
  color: var(--window-text);
  -webkit-font-smoothing: none;
  -moz-osx-font-smoothing: grayscale;
  font-smooth: never;
  text-rendering: optimizeSpeed;
  cursor: default;
}
```

**Bevel utility classes** (4 variants, exactly matching STACK.md recipes):
- `.raised` -- buttons, window frames, toolbars (highlight/light on top-left, shadow/dk-shadow on bottom-right)
- `.sunken` -- text fields, list views, status bars (reversed)
- `.pressed` -- active/clicked buttons (same shadows as sunken but with button-face background)
- `.well` -- status bar panels, group boxes (simple 1px inset)

Use the exact box-shadow values from RESEARCH.md:
```css
.raised {
  box-shadow: inset -1px -1px var(--button-dk-shadow),
              inset  1px  1px var(--button-highlight),
              inset -2px -2px var(--button-shadow),
              inset  2px  2px var(--button-light);
  background: var(--button-face);
}
.sunken {
  box-shadow: inset -1px -1px var(--button-highlight),
              inset  1px  1px var(--button-shadow),
              inset -2px -2px var(--button-light),
              inset  2px  2px var(--button-dk-shadow);
  background: var(--window-bg);
}
.pressed {
  box-shadow: inset -1px -1px var(--button-highlight),
              inset  1px  1px var(--button-shadow),
              inset -2px -2px var(--button-light),
              inset  2px  2px var(--button-dk-shadow);
  background: var(--button-face);
}
.well {
  box-shadow: inset -1px -1px var(--button-highlight),
              inset  1px  1px var(--button-shadow);
}
```

**NT4 button styles:**
```css
.nt4-button {
  /* inherits .raised via box-shadow */
  box-shadow: inset -1px -1px var(--button-dk-shadow),
              inset  1px  1px var(--button-highlight),
              inset -2px -2px var(--button-shadow),
              inset  2px  2px var(--button-light);
  background: var(--button-face);
  border: none;
  padding: 4px 12px;
  font-family: inherit;
  font-size: 11px;
  cursor: default;
  min-width: 75px;
  text-align: center;
}
.nt4-button:active {
  box-shadow: inset -1px -1px var(--button-highlight),
              inset  1px  1px var(--button-shadow),
              inset -2px -2px var(--button-light),
              inset  2px  2px var(--button-dk-shadow);
  padding: 5px 11px 3px 13px; /* shift text down-right 1px */
}
.nt4-button:focus {
  outline: 1px dotted var(--window-text);
  outline-offset: -4px;
}
```

**Window chrome styles** (for login dialog now, window manager in Phase 2):
```css
.nt4-window {
  background: var(--button-face);
  box-shadow: inset -1px -1px var(--button-dk-shadow),
              inset  1px  1px var(--button-highlight),
              inset -2px -2px var(--button-shadow),
              inset  2px  2px var(--button-light);
  position: absolute;
}
.nt4-titlebar {
  background: var(--active-title);
  color: var(--active-title-text);
  padding: 2px 3px;
  font-weight: bold;
  font-size: 11px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  user-select: none;
}
.nt4-titlebar.inactive {
  background: var(--inactive-title);
  color: var(--inactive-title-text);
}
.nt4-titlebar-buttons {
  display: flex;
  gap: 2px;
}
.nt4-titlebar-btn {
  width: 16px;
  height: 14px;
  background: var(--button-face);
  box-shadow: inset -1px -1px var(--button-dk-shadow),
              inset  1px  1px var(--button-highlight),
              inset -2px -2px var(--button-shadow),
              inset  2px  2px var(--button-light);
  border: none;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: default;
  font-size: 8px;
  line-height: 1;
}
.nt4-titlebar-btn:active {
  box-shadow: inset -1px -1px var(--button-highlight),
              inset  1px  1px var(--button-shadow),
              inset -2px -2px var(--button-light),
              inset  2px  2px var(--button-dk-shadow);
}
```

**Window button glyphs** -- CSS-drawn, NOT text characters:
- Minimize: a small horizontal line near bottom (use a pseudo-element, 6px wide, 1px tall, positioned near bottom-center)
- Maximize: a small rectangle outline (use border on pseudo-element, ~7px square)
- Close: a small X (use two rotated pseudo-elements crossing, or use the multiplication sign character at small size)

**Dialog styles** (for login dialog and viewport gate):
```css
.nt4-dialog {
  background: var(--button-face);
  box-shadow: inset -1px -1px var(--button-dk-shadow),
              inset  1px  1px var(--button-highlight),
              inset -2px -2px var(--button-shadow),
              inset  2px  2px var(--button-light);
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 1000;
}
.dialog-titlebar {
  background: var(--active-title);
  color: var(--active-title-text);
  padding: 2px 4px;
  font-weight: bold;
  font-size: 11px;
  user-select: none;
}
.dialog-body {
  padding: 12px 16px;
}
.dialog-buttons {
  padding: 8px 16px 12px;
  display: flex;
  justify-content: center;
  gap: 6px;
}
```

**Form field styles** (for login dialog):
```css
.nt4-input {
  box-shadow: inset -1px -1px var(--button-highlight),
              inset  1px  1px var(--button-shadow),
              inset -2px -2px var(--button-light),
              inset  2px  2px var(--button-dk-shadow);
  background: var(--window-bg);
  border: none;
  padding: 2px 4px;
  font-family: inherit;
  font-size: 11px;
}
.nt4-select {
  box-shadow: inset -1px -1px var(--button-highlight),
              inset  1px  1px var(--button-shadow),
              inset -2px -2px var(--button-light),
              inset  2px  2px var(--button-dk-shadow);
  background: var(--window-bg);
  border: none;
  padding: 2px;
  font-family: inherit;
  font-size: 11px;
}
```

**Desktop background:**
```css
#desktop {
  width: 100vw;
  height: 100vh;
  background: var(--desktop);
  position: relative;
  overflow: hidden;
}
```

**Boot screen backgrounds:**
```css
#boot-screen {
  width: 100vw;
  height: 100vh;
  position: relative;
}
.boot-post {
  background: #000000;
  color: #AAAAAA;
  font-family: "Courier New", Courier, monospace;
  font-size: 14px;
  padding: 20px;
  width: 100%;
  height: 100%;
  white-space: pre;
  line-height: 1.4;
}
.boot-ntldr {
  background: #000000;
  color: #AAAAAA;
  font-family: "Courier New", Courier, monospace;
  font-size: 14px;
  padding: 20px;
  width: 100%;
  height: 100%;
  white-space: pre;
  line-height: 1.4;
}
.boot-ntldr .highlight {
  background: var(--active-title-text);
  color: var(--active-title);
}
.boot-starting {
  background: var(--active-title);
  color: var(--active-title-text);
  font-family: "Courier New", Courier, monospace;
  font-size: 16px;
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  line-height: 1.6;
}
.boot-security {
  background: var(--active-title);
  color: var(--active-title-text);
  font-family: inherit;
  font-size: 14px;
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}
.boot-login {
  background: var(--desktop);
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

**Viewport gate styles:**
```css
.viewport-gate {
  min-width: 350px;
  max-width: 420px;
}
.viewport-gate .dialog-body {
  display: flex;
  gap: 12px;
  align-items: flex-start;
}
.viewport-gate .dialog-icon {
  font-size: 32px;
  flex-shrink: 0;
}
```

**Loading settings screen** (for return visitors):
```css
.boot-loading-settings {
  background: var(--desktop);
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--active-title-text);
  font-size: 14px;
}
```

**Scrollbar styles** (cosmetic, for supported browsers):
```css
::-webkit-scrollbar { width: 16px; height: 16px; }
::-webkit-scrollbar-track { background: var(--scrollbar); }
::-webkit-scrollbar-thumb {
  background: var(--button-face);
  box-shadow: inset -1px -1px var(--button-dk-shadow),
              inset  1px  1px var(--button-highlight),
              inset -2px -2px var(--button-shadow),
              inset  2px  2px var(--button-light);
}
::-webkit-scrollbar-button { display: block; height: 16px; width: 16px; background: var(--button-face); }
```

Do NOT include any gradients. Do NOT use #D4D0C8. Do NOT use border-style: outset/inset for bevels.
  </action>
  <verify>
    <automated>grep -c "box-shadow: inset" C:/Users/Taylor/Desktop/Projects/github/lockdown-rime/public/css/nt4.css</automated>
  </verify>
  <acceptance_criteria>
    - nt4.css contains `--button-face: #C0C0C0` (not #D4D0C8)
    - nt4.css contains `--active-title: #000080`
    - nt4.css contains `--desktop: #008080`
    - nt4.css contains `box-shadow: inset` (at least 10 occurrences for bevel classes)
    - nt4.css contains `.raised` class with 4-shadow box-shadow
    - nt4.css contains `.sunken` class
    - nt4.css contains `.nt4-button` class
    - nt4.css contains `.nt4-window` class
    - nt4.css contains `.nt4-titlebar` class
    - nt4.css contains `font-family: "W95FA"` in @font-face
    - nt4.css contains `-webkit-font-smoothing: none`
    - nt4.css contains `#boot-screen` styles
    - nt4.css contains `.boot-post` with `background: #000000`
    - nt4.css contains `.boot-starting` with `background: var(--active-title)`
    - nt4.css contains `.viewport-gate` class
    - nt4.css does NOT contain the string "gradient"
    - nt4.css does NOT contain #D4D0C8
    - Font files exist at public/assets/fonts/w95fa.woff2 and w95fa.woff (or fallback note if download fails)
  </acceptance_criteria>
  <done>nt4.css is a complete NT4 visual system with all color variables, bevel classes, window chrome, button styles, dialog styles, boot screen backgrounds, form fields, scrollbar styling, and viewport gate styles. W95FA font files are present. No Win98 visual elements (no gradients, no warm grey).</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html and desktop.html with correct structure, create time.js</name>
  <files>public/index.html, public/desktop.html, public/js/time.js</files>
  <read_first>
    - public/index.html (current placeholder)
    - public/desktop.html (current placeholder)
    - .planning/phases/01-foundation/01-RESEARCH.md (TimeEngine code, viewport gate code)
  </read_first>
  <action>
**index.html -- Rewrite with these exact elements:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=800, initial-scale=1.0">
  <title>CalCom Workstation</title>
  <link rel="stylesheet" href="css/nt4.css">
</head>
<body>
  <div id="boot-screen"></div>
  <div id="viewport-gate" style="display: none;"></div>
  <script src="js/boot.js"></script>
</body>
</html>
```

Key details:
- Meta viewport uses `width=800` not `device-width` (desktop-only experience)
- boot-screen div is the container for all boot stages (boot.js will populate it)
- viewport-gate div is hidden by default (boot.js shows it if viewport is too small)
- Only boot.js is loaded (not desktop.js or apps.js)

**desktop.html -- Rewrite with teal background and time.js:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=800, initial-scale=1.0">
  <title>CalCom Workstation</title>
  <link rel="stylesheet" href="css/nt4.css">
</head>
<body>
  <div id="desktop"></div>
  <script src="js/time.js"></script>
  <script src="js/desktop.js"></script>
  <script src="js/apps.js"></script>
</body>
</html>
```

Key details:
- Same viewport meta
- `#desktop` div gets teal background from CSS (`background: var(--desktop)`)
- time.js loaded before desktop.js so TimeEngine is available for the clock
- desktop.js and apps.js remain as placeholders for now

**time.js -- Create TimeEngine:**

```javascript
// TimeEngine -- maps real-world time to December 23, 1999
// 1:1 mapping: current hour/minute/second maps to same time on Dec 23, 1999

const TimeEngine = {
  ANCHOR_YEAR: 1999,
  ANCHOR_MONTH: 11, // December (0-indexed)
  ANCHOR_DAY: 23,

  getMappedTime() {
    const now = new Date();
    return new Date(
      this.ANCHOR_YEAR,
      this.ANCHOR_MONTH,
      this.ANCHOR_DAY,
      now.getHours(),
      now.getMinutes(),
      now.getSeconds()
    );
  },

  formatClockTime(date) {
    let hours = date.getHours();
    const minutes = date.getMinutes().toString().padStart(2, '0');
    const ampm = hours >= 12 ? 'PM' : 'AM';
    hours = hours % 12 || 12;
    return hours + ':' + minutes + ' ' + ampm;
  },

  formatFullDate(date) {
    const days = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];
    const months = ['January','February','March','April','May','June',
                    'July','August','September','October','November','December'];
    return days[date.getDay()] + ', ' + months[date.getMonth()] + ' ' + date.getDate() + ', ' + date.getFullYear();
  },

  isAfter(deliverAt) {
    // Check if the current mapped time is past the deliver_at timestamp
    const mapped = this.getMappedTime();
    const target = new Date(deliverAt);
    return mapped >= target;
  }
};
```

The TimeEngine is a plain object (not a class, not a module) because this is vanilla JS loaded via script tags. It must be globally accessible.

The `isAfter` method is for email time-gating in Phase 3 -- include it now so the API is complete.
  </action>
  <verify>
    <automated>grep -l "boot-screen" C:/Users/Taylor/Desktop/Projects/github/lockdown-rime/public/index.html && grep -l "TimeEngine" C:/Users/Taylor/Desktop/Projects/github/lockdown-rime/public/js/time.js && grep -l "id=\"desktop\"" C:/Users/Taylor/Desktop/Projects/github/lockdown-rime/public/desktop.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="boot-screen"`
    - index.html contains `id="viewport-gate"`
    - index.html contains `src="js/boot.js"`
    - index.html does NOT contain `src="js/desktop.js"`
    - desktop.html contains `id="desktop"`
    - desktop.html contains `src="js/time.js"` before `src="js/desktop.js"`
    - desktop.html contains `src="js/desktop.js"`
    - time.js contains `TimeEngine`
    - time.js contains `getMappedTime`
    - time.js contains `formatClockTime`
    - time.js contains `formatFullDate`
    - time.js contains `1999` and `ANCHOR_DAY: 23`
    - time.js contains `isAfter`
  </acceptance_criteria>
  <done>index.html has boot-screen container and viewport-gate div. desktop.html shows teal desktop via CSS and loads time.js before desktop.js. time.js provides TimeEngine with getMappedTime (returns Dec 23 1999 with current hour/minute/second), formatClockTime (12-hour AM/PM), formatFullDate (full weekday/month/day/year), and isAfter (for time-gated content).</done>
</task>

</tasks>

<verification>
1. Open public/index.html in browser -- should show empty black screen (boot.js is still a placeholder, but no errors in console)
2. Open public/desktop.html in browser -- should show solid teal (#008080) background filling the viewport
3. In browser console on desktop.html: `TimeEngine.formatClockTime(TimeEngine.getMappedTime())` returns current time in AM/PM format
4. In browser console: `TimeEngine.formatFullDate(TimeEngine.getMappedTime())` returns "Thursday, December 23, 1999"
5. grep confirms no gradients in nt4.css
6. grep confirms no #D4D0C8 in nt4.css
</verification>

<success_criteria>
- nt4.css contains the complete NT4 color palette, bevel classes, window chrome, buttons, dialogs, form fields, boot screen backgrounds, and viewport gate styles
- All CSS uses box-shadow inset for bevels (not border-style: outset)
- No Win98 visual elements anywhere (no gradients, no warm grey)
- W95FA font loaded via @font-face with WOFF2/WOFF
- Font anti-aliasing disabled for pixel-crisp rendering
- index.html structured for boot sequence with viewport gate
- desktop.html shows teal background and loads TimeEngine
- TimeEngine correctly maps real time to December 23, 1999
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
