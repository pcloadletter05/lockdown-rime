---
phase: 06-december-1998-date-shift-ask-jeeves-browser-calcom-logo-and-intranet-branding-fixes
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - public/js/browser.js
  - public/js/boot.js
  - public/content/bookmarks.json
autonomous: true
requirements:
  - AJ-01
  - BRAND-01
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "Visitor can navigate to Ask Jeeves page via bookmark in browser favorites"
    - "Ask Jeeves page renders with period-authentic late-90s search engine layout"
    - "CalCom logo appears in intranet page header alongside company name"
    - "CalCom logo appears in corporate page header"
    - "CalCom logo appears in login dialog above Windows NT text"
  artifacts:
    - path: "public/js/browser.js"
      provides: "Ask Jeeves BROWSER_PAGES entry and CalCom logo in intranet/corporate headers"
      contains: "ask-jeeves"
    - path: "public/js/boot.js"
      provides: "CalCom logo in login dialog"
      contains: "calcom_logo.png"
    - path: "public/content/bookmarks.json"
      provides: "Ask Jeeves top-level bookmark entry"
      contains: "Ask Jeeves"
  key_links:
    - from: "public/content/bookmarks.json"
      to: "public/js/browser.js"
      via: "bookmark url key maps to BROWSER_PAGES['ask-jeeves']"
      pattern: "ask-jeeves"
    - from: "public/js/boot.js"
      to: "public/assets/calcom/calcom_logo.png"
      via: "img src in login dialog HTML"
      pattern: "calcom_logo.png"
---

<objective>
Add an Ask Jeeves static homepage to the browser and embed CalCom logo branding into the intranet page header, corporate page header, and login dialog. The Ask Jeeves page is pure period dressing with no narrative significance -- a decorative search engine page bookmarked because any 1998 office worker would have it. The CalCom branding creates a corporate-managed workstation feel.

Purpose: Period authenticity (Ask Jeeves) and corporate identity (CalCom branding).
Output: New browser page, new bookmark, logo in 3 locations.
</objective>

<execution_context>
@C:/Users/Taylor/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Taylor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-december-1998-date-shift-ask-jeeves-browser-calcom-logo-and-intranet-branding-fixes/06-CONTEXT.md
@.planning/phases/06-december-1998-date-shift-ask-jeeves-browser-calcom-logo-and-intranet-branding-fixes/06-RESEARCH.md
@.planning/phases/06-december-1998-date-shift-ask-jeeves-browser-calcom-logo-and-intranet-branding-fixes/06-01-SUMMARY.md

<interfaces>
<!-- Key patterns from browser.js that executor must follow -->

From public/js/browser.js - BROWSER_PAGES object pattern:
```javascript
var BROWSER_PAGES = {
  'page-key': {
    title: 'Page Title',
    url: 'http://www.example.com/',
    content: '<table width="100%" ...>...</table>'
  },
  // ... more pages
};
```

From public/content/bookmarks.json - bookmark entry pattern:
```json
{
  "name": "Display Name",
  "url": "page-key",
  "icon": null,
  "children": null
}
```

From public/js/boot.js - login dialog HTML structure (line 267-299):
```javascript
async function loginDialog() {
  var dialogHTML =
    '<div class="nt4-dialog" style="width: 340px;">' +
      '<div class="dialog-titlebar"><span>Logon Information</span></div>' +
      '<div class="dialog-body">' +
        '<div style="background: var(--active-title); color: white; padding: 8px 12px; margin-bottom: 12px; text-align: center; font-weight: bold;">' +
          'Windows NT' +
        '</div>' +
        // ... form fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ask Jeeves browser page and bookmark</name>
  <read_first>
    - public/js/browser.js (BROWSER_PAGES object -- understand exact pattern for page entries, especially yahoo-portal for layout reference)
    - public/content/bookmarks.json (bookmark structure and current entries)
  </read_first>
  <files>
    public/js/browser.js,
    public/content/bookmarks.json
  </files>
  <action>
    **Add Ask Jeeves page to BROWSER_PAGES in browser.js:**

    Add a new entry with key `'ask-jeeves'` to the BROWSER_PAGES object, placed after `'yahoo-portal'` and before `'404'`. The page should be a period-authentic late-1998 Ask Jeeves homepage with:

    - White background, centered content, table-based layout (matching yahoo-portal pattern)
    - Header area with "Ask Jeeves!" title in large font (serif face like Georgia or Times New Roman, color scheme: use a combination of red and blue text or keep it simple with dark blue)
    - A butler character representation -- since this is non-commercial, include a reference to the Jeeves character. Options: use a text description placeholder like "[Butler illustration]" styled to look like an image area, OR create a simple inline representation. Given that copyrighted imagery is OK per user decision, use an `<img>` tag pointing to `assets/ask-jeeves/jeeves.png` -- the executor should create or source a simple butler character image. If sourcing proves difficult, use a text-based representation styled in a box.
    - Natural language prompt text: "You may type in your question below. For example: Where can I find information about Y2K?"
    - Decorative search input box and "Ask!" button (non-functional, consistent with how Yahoo search box works)
    - "You may also ask:" section with 3-4 example question links (non-functional `href="#"`), period-appropriate topics like:
      - "What is the weather forecast for New York?"
      - "Where can I find free email?"
      - "How do I create a web page?"
      - "What are the best Christmas gifts for 1998?"
    - Footer: `Copyright &copy; 1998 Ask Jeeves, Inc. All rights reserved.`
    - URL: `http://www.askjeeves.com/`
    - Title: `Ask Jeeves!`

    Use `<table>`, `<font>`, `<center>`, `bgcolor` attributes -- period-authentic HTML like the existing pages.

    **Add Ask Jeeves bookmark to bookmarks.json:**

    Add a new top-level entry in the `items` array, placed after "Yahoo!" (last entry):
    ```json
    {
      "name": "Ask Jeeves",
      "url": "ask-jeeves",
      "icon": null,
      "children": null
    }
    ```
  </action>
  <verify>
    <automated>cd /c/Users/Taylor/Desktop/Projects/github/lockdown-rime && grep -n "ask-jeeves" public/js/browser.js public/content/bookmarks.json && node -e "JSON.parse(require('fs').readFileSync('public/content/bookmarks.json','utf8')); console.log('bookmarks.json valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `browser.js` contains a BROWSER_PAGES entry with key `'ask-jeeves'`
    - `browser.js` Ask Jeeves entry has `title: 'Ask Jeeves!'`
    - `browser.js` Ask Jeeves entry has `url: 'http://www.askjeeves.com/'`
    - `browser.js` Ask Jeeves entry has `content:` with table-based HTML layout
    - `browser.js` Ask Jeeves content contains "Ask!" button text
    - `browser.js` Ask Jeeves content contains `1998` copyright (NOT 1999)
    - `bookmarks.json` contains `"name": "Ask Jeeves"` with `"url": "ask-jeeves"`
    - `bookmarks.json` remains valid JSON
  </acceptance_criteria>
  <done>Ask Jeeves page renders in the browser when navigated to via bookmark. Page has period-authentic late-90s search engine layout with decorative search box, example questions, and 1998 copyright.</done>
</task>

<task type="auto">
  <name>Task 2: Add CalCom logo to intranet, corporate, and login dialog</name>
  <read_first>
    - public/js/browser.js (calcom-intranet header at lines 9-13, calcom-corporate header at lines 64-68)
    - public/js/boot.js (login dialog at lines 265-300, navy banner div at lines 270-272)
    - public/assets/calcom/calcom_logo.png (check image dimensions for sizing)
  </read_first>
  <files>
    public/js/browser.js,
    public/js/boot.js
  </files>
  <action>
    **CalCom Intranet page header (browser.js):**

    In the `calcom-intranet` BROWSER_PAGES entry, update the navy header `<td>` (currently just text "CalCom Research Services") to include the logo before the text. Replace the header cell content:

    FROM:
    ```
    '<td colspan="2" bgcolor="#000080" style="padding: 10px 20px;">' +
      '<font color="#FFFFFF" size="5" face="Arial"><b>CalCom Research Services</b></font><br>' +
      '<font color="#CCCCFF" size="2" face="Arial">Internal Network - Authorized Users Only</font>' +
    '</td>'
    ```

    TO:
    ```
    '<td colspan="2" bgcolor="#000080" style="padding: 10px 20px;">' +
      '<img src="assets/calcom/calcom_logo.png" width="120" style="vertical-align: middle; margin-right: 12px; background: white; padding: 4px;">' +
      '<font color="#FFFFFF" size="5" face="Arial"><b>CalCom Research Services</b></font><br>' +
      '<font color="#CCCCFF" size="2" face="Arial">Internal Network - Authorized Users Only</font>' +
    '</td>'
    ```

    The logo gets a small white background padding since it has a white/light background that would blend into the navy bar. Width 120px keeps it proportional to the header text.

    **CalCom Corporate page header (browser.js):**

    In the `calcom-corporate` BROWSER_PAGES entry, update the dark blue header to include the logo. Replace:

    FROM:
    ```
    '<tr><td bgcolor="#003366" style="padding: 15px 20px;">' +
      '<font color="#FFFFFF" size="5" face="Georgia"><b>CalCom</b></font>' +
      '<font color="#99CCFF" size="3" face="Georgia"> Research Services</font>' +
    '</td></tr>'
    ```

    TO:
    ```
    '<tr><td bgcolor="#003366" style="padding: 15px 20px;">' +
      '<img src="assets/calcom/calcom_logo.png" width="140" style="vertical-align: middle; margin-right: 15px; background: white; padding: 4px;">' +
      '<font color="#FFFFFF" size="5" face="Georgia"><b>CalCom</b></font>' +
      '<font color="#99CCFF" size="3" face="Georgia"> Research Services</font>' +
    '</td></tr>'
    ```

    Width 140px for the corporate site (slightly larger than intranet to match the more spacious corporate layout).

    **Login dialog CalCom branding (boot.js):**

    In the `loginDialog()` function, update the navy banner div to include the CalCom logo above the "Windows NT" text. Replace:

    FROM:
    ```
    '<div style="background: var(--active-title); color: white; padding: 8px 12px; margin-bottom: 12px; text-align: center; font-weight: bold;">' +
      'Windows NT' +
    '</div>'
    ```

    TO:
    ```
    '<div style="background: var(--active-title); color: white; padding: 8px 12px; margin-bottom: 12px; text-align: center; font-weight: bold;">' +
      '<img src="assets/calcom/calcom_logo.png" width="80" style="display: block; margin: 0 auto 6px; background: white; padding: 3px;"><br>' +
      'Windows NT' +
    '</div>'
    ```

    The logo is 80px wide in the login dialog (smaller, subtle corporate branding -- not a splash screen). White background padding ensures visibility against the navy banner. The `display: block; margin: 0 auto` centers it above the "Windows NT" text.
  </action>
  <verify>
    <automated>cd /c/Users/Taylor/Desktop/Projects/github/lockdown-rime && grep -n "calcom_logo.png" public/js/browser.js public/js/boot.js</automated>
  </verify>
  <acceptance_criteria>
    - `browser.js` calcom-intranet section contains `calcom_logo.png` with `width="120"`
    - `browser.js` calcom-corporate section contains `calcom_logo.png` with `width="140"`
    - `boot.js` loginDialog function contains `calcom_logo.png` with `width="80"`
    - `grep -c "calcom_logo.png" public/js/browser.js` returns 2 (intranet + corporate)
    - `grep -c "calcom_logo.png" public/js/boot.js` returns 1 (login dialog)
    - Logo img tags all include `style` with `background: white` for visibility on dark backgrounds
  </acceptance_criteria>
  <done>CalCom logo appears in three locations: intranet page header (120px), corporate page header (140px), and login dialog (80px). All three render the logo with white background padding against dark banner backgrounds.</done>
</task>

</tasks>

<verification>
1. Navigate to Ask Jeeves via browser bookmark -- page renders with search box and period layout
2. CalCom intranet page shows logo in navy header bar
3. CalCom corporate page shows logo in dark blue header bar
4. Login dialog shows CalCom logo above "Windows NT" text
5. `grep -rn "calcom_logo.png" public/js/` returns 3 hits (2 in browser.js, 1 in boot.js)
6. `grep -n "ask-jeeves" public/js/browser.js public/content/bookmarks.json` returns hits in both files
</verification>

<success_criteria>
1. Ask Jeeves page exists in BROWSER_PAGES and is reachable via bookmark
2. Ask Jeeves page has period-authentic layout with decorative search box
3. CalCom logo visible in intranet header, corporate header, and login dialog
4. All three logo placements use appropriate sizes (80/120/140px)
5. Bookmarks.json includes Ask Jeeves as top-level entry
</success_criteria>

<output>
After completion, create `.planning/phases/06-december-1998-date-shift-ask-jeeves-browser-calcom-logo-and-intranet-branding-fixes/06-02-SUMMARY.md`
</output>
